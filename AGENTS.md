# Prompt2Prompt — AGENTS.md

Empty repository. No code, config, or tooling has been initialized yet.

## Skills

- `.opencode/skills/agent-prompt-composer/SKILL.md` — Load this skill when composing a prompt for another agent. It gathers context (AGENTS.md, available skills, model info, project config) and produces structured prompts.

## Before starting any work

- Install language toolchain, create a lockfile, and set up a build/test/lint pipeline before writing source code.
- Add developer commands (`dev`, `build`, `test`, `lint`, `typecheck`) to `package.json` / equivalent as soon as a framework is chosen.
- Keep this file updated as conventions solidify.
